name: GitHub Action do zbudowania obrazu i umieszczeniu go na DockerHub

on:
  push:
    branches: [ master ]

jobs:
  build-push-images:
    name: Budowa obrazu i publikacja na repozytorium
    runs-on: ubuntu-latest

    steps:
      
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Bulidx
        id: buildx
        uses: docker/setup-buildx-action@v2
        
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          context: ./
          platforms: linux/arm/v7,linux/arm64/v8,linux/amd64
          file: ./Dockerfile2
          push: true
          tags: svikt02/zadanie1:v1.0
          cache-from: type=registry,ref=svikt02/zadanie1:v1.0
          cache-to: type=registry,ref=svikt02/zadanie1:v1.0,mode=max

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
          echo test, and deploy your project.
